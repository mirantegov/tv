// Gerado em runtime pelo entrypoint (envsubst). NÃO editar no container.
window.__TENANT__ = {
	id: "${TENANT_ID}",
	slug: "${TENANT_SLUG}",
	nome: "${TENANT_NOME}",
	uf: "${TENANT_UF}",
};
window.__API_URL__ = "${API_URL}";
