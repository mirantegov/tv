node_modules/
dist/
*.local.md
.serena/
.claude/

# Playwright
test-results/
playwright-report/

# Env por tenant (contém senha) — versionar só os .example
.env
.env.*
!.env.example
!.env.<tenant>.example
!.env.warehouse.example

# Caddyfile gerado pelo deploy.sh (não versionar)
deploy/Caddyfile

# OpenTofu / Terraform
**/.terraform/
*.tfstate
*.tfstate.*

# Scripts locais de diagnóstico (podem conter segredos) — não versionar
scripts/

# Terraform/OpenTofu tfvars (contêm segredos) — versionar só os .example
*.tfvars
!*.tfvars.example
